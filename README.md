# Offset-Angle-Glyphs-3-Macro-Python-Script
Offsets a glyphs path that uses angles. Fixing the problems with offset curve tool. Allowing for new layers, flattening, single, batch of glyphs or entire font. Works for geometric and angle based fonts.
